# Admin UI Environment Variables

# Admin authentication token (must match BUSINESS_CONTEXT_ADMIN_TOKEN in parent API)
# Leave empty for development (no auth required)
# Set in production for security
NEXT_PUBLIC_ADMIN_TOKEN=

# Only needed if deploying admin separately from main API
# Leave empty if deployed in same Vercel project
# VERCEL_URL=https://your-api-domain.vercel.app
