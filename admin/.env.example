# Admin UI Environment Variables

# API Base URL - points to main API server
# Development: http://localhost:3000
# Production: Leave empty (uses relative URLs on same domain)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Admin authentication token (must match ADMIN_API_TOKEN in main API project)
# Leave empty for development (no auth required)
# Set in production for security
# NEXT_PUBLIC_ADMIN_TOKEN=
